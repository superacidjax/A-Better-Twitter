- if signed_in?
  .row
    %aside.span4
      %section
        = render 'shared/user_info'
      %section
        = render 'shared/stats'
      %section
        = render 'shared/note_form'
      %section
        = render 'shared/user_group_ownership'
      %section
        = link_to "Create a group", new_group_path
    .span8
      %h3 People you follow
      = render partial: 'shared/feed'
      %h3
        In your groups
      = render partial: 'groups/feed'
